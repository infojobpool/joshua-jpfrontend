/**
 * Open task discovery — must stay in sync with `MobileBottomNav` “Browse”.
 */
export const HOME_BROWSE_ALL_TASKS_HREF = "/dashboard" as const;

export const HOME_EXPLORE_ALL_TASKS_LABEL = "Browse all tasks" as const;
