# Must be an absolute URL including /api/v1 (no trailing slash).
# Wrong: https://api.jobpool.in  → 404 on admin-login (missing /api/v1)
# Wrong: /api/v1  → requests go to your Vercel admin domain, not the API
# Example:
NEXT_PUBLIC_API_BASE_URL=https://api.jobpool.in/api/v1
